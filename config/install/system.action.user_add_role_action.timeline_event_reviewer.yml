langcode: en
status: true
dependencies:
  config:
    - user.role.timeline_event_reviewer
  module:
    - user
id: user_add_role_action.timeline_event_reviewer
label: 'Add the Timeline Event Reviewer role to the selected users'
type: user
plugin: user_add_role_action
configuration:
  rid: timeline_event_reviewer
